-cp src
-js artifacts/js/<%= appNameSlug %>.js

# this lines forces the inclusion of all files under the src dir
# this is most useful in the case of libs with no "main"
--macro include('', true, null, ['src'])
